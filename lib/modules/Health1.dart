import 'package:flutter/material.dart';

class Health1 extends StatelessWidget {
  const Health1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor: Colors.blue[200],
        title: Text("استشارات  طبية"),
      ),
    );
  }
}
